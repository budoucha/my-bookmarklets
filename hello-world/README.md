# Hello World

A simple "Hello World" bookmarklet that displays a greeting alert when clicked.

## Usage

Click this bookmarklet while on any webpage to display a friendly greeting message.

## How it works

The bookmarklet calls `alert()` with a greeting message and the current page URL.
